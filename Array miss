#include <stdio.h>
#include <stdlib.h>

int main() {
    int size, arr[10], i, j, found;
    
    printf("Enter array size: ");
    scanf("%d", &size);
    
    printf("Enter 10 array elements: ");
    for(i = 0; i < 10; i++) {
        scanf("%d", &arr[i]);
    }
    
    printf("Missing elements: ");
    for(i = 1; i <= size; i++) {
        found = 0;
        for(j = 0; j < 10; j++) {
            if(arr[j] == i) {
                found = 1;
                break;
            }
        }
        if(!found) {
            printf("%d ", i);
        }
    }
    
    return 0;
}
